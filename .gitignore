# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Python
__pycache__/
*.pyc
.venv/
venv/

# AWS
.aws/

# Personal interview prep — deliberately never committed. Lives outside the
# repo, in "SRE project study guides and extra info". This entry exists so a
# stray docs/interview/ can never be added back by accident.
docs/interview/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
